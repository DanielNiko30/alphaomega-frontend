<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🔹 Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="${applicationName}"
        android:label="frontend_new"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <!-- ✅ OneSignal setup -->
        <meta-data
            android:name="onesignal_app_id"
            android:value="257845e8-86e4-466e-b8cb-df95a1005a5f"/>

        <meta-data
            android:name="onesignal_google_project_number"
            android:value="REMOTE"/>

        <!-- ✅ Penting agar OneSignal bisa tampilkan notif walau app terminated -->
        <meta-data
            android:name="com.onesignal.NotificationServiceExtension"
            android:value="com.onesignal.OneSignalNotificationServiceExtension"/>

        <!-- ✅ Channel untuk suara custom -->
        <meta-data
            android:name="com.onesignal.NotificationChannel.DEFAULT"
            android:value="default"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme sementara saat Flutter init -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter generated plugin registrant -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 🔹 Query intent tambahan (standar Flutter) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
